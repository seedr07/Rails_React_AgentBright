<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "AgentBright" %></title>

  <%= csrf_meta_tags %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
  <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
  <![endif]-->

  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>

  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>

  <%= yield :head_script  %>

  <%= javascript_include_tag "super_admin" if super_admin_signed_in? %>

  <%= javascript_include_tag "https://js.stripe.com/v2/" %>

  <%= render(partial: "layouts/browser_specific_javascript") %>

  <%= favicon_link_tag 'ab-logo.png', rel: 'apple-touch-icon', type: 'image/png' %>
  <link rel="shortcut icon" href="/abfavicon.png" type="image/x-icon">

</head>
<body id="<%= "%s_%s" % [controller_name, action_name] %> <%= @layout_carrier.env_banner %>">
  <style>
  .wrap {
    background-color: white;
  }
  </style>

  <%= render(partial: "shared/environment_banner") %>
  <%= render(partial: "layouts/account_inactive_sidebar") %>

  <%= yield %>

  <%= render(partial: "application/extra_content") %>

</body>
</html>
