<!DOCTYPE html>
<html lang="en" class="ux-layout">
  <head>
    <%= render(partial: "application/head_contents_ux") %>
  </head>

  <body id="<%= "%s_%s" % [controller_name, action_name] %>" class="<%= @layout_carrier.env_banner %> <%= yield :body_class if content_for? :body_class %> ">

    <%= semantic_flash %>
    <%= yield %>

    <%= render(partial: "application/extra_content_ux") %>
    <%#= redux_store_hydration_data %>
  </body>
</html>
